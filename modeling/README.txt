modeling_V1
--V1_0:makingで使ってたものを参考に新PC購入後初のモデリング。[クラスタリング:なし,次元削減:なし,ボート、艇番号:あり]＝変更点は余計な計算の除去、学習アルゴリズムをXGbiistに変更した。木の種類の変更、割合変更などだけ。
--V1_1:アルゴリズムは変わらずXGboostでやった。特徴量からモータ番号、艇番号を削除した。代わりにクラスタリングラベルを追加した。
--V1_2:（未作成）
--


modeling_V2
--V2_0:アルゴリズムをrandom forest に変更して、特徴量はクラスタリングを持ったバージョンを使用、
--V2_1:アルゴリズムはXGboost 出力を0~1の確率で出力するように変更、precisionを上げることを目的として閾値の探索要素も追加した。
------V2_1_1:v2_1の結果がおかしい、モデルのチェックを行う。方針としては2019と2020を一切関与させないでもでっリングを行ってみる、それでエラーはいたらどっかで食い込んでいたということ
--V2_2:アルゴリズムはrondom forest 出力を0~1の確率で出力するように変更、precisionを上げることを目的として閾値の探索要素も追加した。
--

modeling_V3:モデリング時のスコアシートの形式を変換したもの（モデル選定をランダムフォレストに変えるため）
--V3_1:スコアシート変更後の最初のバージョン